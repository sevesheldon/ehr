select target code to active code-specific validation rules

create scoring models.

activate functions to update current score when user interacts with form

as user interacts with form inputs, scores are updated, then tested against target model

when they meet a requirement, the final validation object’s corresponding value turns from false to true.

	if all validation aspects become true, target code is valid